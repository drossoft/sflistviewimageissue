<?xml version="1.0" encoding="UTF-8" ?>
<Shell
	x:Class="sflistviewimageissue.AppShell"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:views="clr-namespace:sflistviewimageissue.Views"
	xmlns:local="clr-namespace:sflistviewimageissue">

	<TabBar>
		<ShellContent
			Title="SfListView"
			Icon="iconblank.png"
			ContentTemplate="{DataTemplate views:Page1Page}" />
        <ShellContent
			Title="Blank"
			Icon="iconblank.png"
			ContentTemplate="{DataTemplate views:Page2Page}" />
    </TabBar>
</Shell>
